#define SONAR S4
#define NEAR 15


task main()
{
 SetSensorLowspeed(SONAR);
 while(true)
 {
  while(SensorUS(SONAR)<=NEAR);
  OnFwd(OUT_AC,20);
  while(SensorUS(SONAR)>NEAR);
  Off(OUT_AC);
  OnFwd(OUT_B,10);
 }
}
