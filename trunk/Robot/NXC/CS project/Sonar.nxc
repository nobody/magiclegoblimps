#define SONAR S4
#define NEAR 15 //cm
#define SPEED 20

task main()
{
 SetSensorLowspeed(SONAR);
 while(true)
 {
  OnFwd(OUT_AC,SPEED);
  while(SensorUS(SONAR)>NEAR);
  Off(OUT_AC);
  OnFwd(OUT_B,SPEED);
  while(SensorUS(SONAR)<=NEAR);
  Off(OUT_B);
 }
}
