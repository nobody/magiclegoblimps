task main()
{
 int b = BatteryLevel();
 string sb = NumToStr(b);
 string sp = NumToStr(b/90);
 TextOut(40,LCD_LINE4,StrCat(sp,"%"),true);
 TextOut(30,LCD_LINE5,StrCat(sb,"mV"));
 while(!ButtonPressed(BTNCENTER,true));
}
